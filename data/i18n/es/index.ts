import ptBrDict from '../pt-br';
import type { Dictionary } from '../types';

/**
 * Spanish dictionary — temporary stub using pt-BR.
 * Will be replaced with real translations in Lote 4.
 */
const dict: Dictionary = { ...ptBrDict };

export default dict;
