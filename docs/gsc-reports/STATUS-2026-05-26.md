# GSC / Indexação — Status 2026-05-26

> Goal: indexação completa de ulissesflores.com em todos os idiomas. Este relatório
> documenta a sessão de 2026-05-26 (Fases A/B/D parciais). Monitoramento D+3/7/14 e o
> critério ≥90%/idioma dependem do **timeline de crawl do Google** (bloqueio externo).

## Locales (detectados dinamicamente)
`pt-br` (padrão, sem prefixo), `en`, `es`, `it`, `he` — fonte: `data/i18n.ts` + `middleware.ts`.

## Diagnóstico (GSC URL Inspection ao vivo)

| Página | pt-BR | Localizadas (en/es/it/he) |
|---|---|---|
| Homepage | ✅ indexada | ✅ (en, he indexadas; it/identidade ok) |
| /consultoria | 🔴 desconhecida | 🔴 en/es desconhecidas |
| /palestras | 🔴 desconhecida | 🔴 desconhecidas |
| /research (índice) | ✅ indexada | 🔴 /en/research desconhecida |
| /research/<artigo> | parte ✅, parte 🟡 | 🔴 maioria desconhecida |
| /acervo (índice) | 🟡 rastreada não indexada | — |

**Causa-raiz primária:** `/consultoria` e `/palestras` eram **órfãs** (zero links internos) →
"URL desconhecida pelo Google" em todos os idiomas. As variantes localizadas profundas
(research/artigos em en/es/it/he) estão sub-indexadas (descoberta fraca + crawl budget).
**Causa secundária:** 18 artigos com conteúdo **boilerplate** (template idêntico) →
"descoberta/rastreada não indexada" (sinal de conteúdo fino) → **escopo Fase 2** (recuperação Drive).

## Feito nesta sessão
- ✅ **Conserto da orfandade (deploy):** categoria "Serviços" (Consultoria + Palestras) no nav
  global (header em toda página), 5 locales, labels reusados da copy autorada. Commit `8f250b4`,
  push `main` (E2E + build verdes). Dá ao Google caminho de crawl pras páginas comerciais.
- ✅ **IndexNow:** 142 URLs (todos os idiomas) submetidas a `api.indexnow.org` →
  Bing/Yandex/Seznam/Naver. (Google **não** usa IndexNow.)
- ✅ **Favicon UF + OG card 1200×630 + fix www canônico** (commits `cb66608`/`67f1886`, sessão anterior).
- ⏳ **Baseline GSC completa** das ~138 URLs canônicas → `baseline-2026-05-26.json` (matriz abaixo).
- ✅ **hreflang** verificado: já completo (`buildLanguageAlternates`, 5 locales + x-default). Não mexido.
- ✅ **Breadcrumbs:** já existem nas páginas profundas. Não recriados.

## Já correto (NÃO mexer — "não consertar o que funciona")
- hreflang / canonical (fix GSC 2026-04-15 aplicado; firewall www/`/pt-br/`/double-locale OK).
- sitemap único com anotações hreflang por entry (padrão SOTA p/ ≤5 locales; per-locale sitemap = gold-plating, descartado).

## Bloqueios externos (documentados)
- **Indexação pelo Google leva dias-semanas.** O critério ≥90%/idioma e os checkpoints
  D+3/7/14 NÃO são atingíveis em 26/05. Os links internos (deploy de hoje) precisam de um
  ciclo de recrawl do Google.
- **CronCreate é session-bound** (não sobrevive ao fim da sessão) → inadequado p/ D+3/7/14.
  **Recomendação:** agendar a re-inspeção via `/schedule` (agente remoto) ou cron de sistema,
  ou rodar manualmente o script de inspeção nas datas.

## Follow-ups recomendados (não bloqueiam o conserto principal)
- Footer + CTA na home pra /consultoria e /palestras (reforço de link interno; nav já cobre descoberta).
- Fase 2: recuperar conteúdo real dos 18 artigos (fontes no Drive) → resolve o "THIN_CONTENT".
- Revisar os labels de nav que propus (Consulting/Talks, Consultoría/Conferencias, Consulenza/Conferenze, ייעוץ/הרצאות).

## Lista priorizada p/ "Solicitar Indexação" manual no GSC
(Ver `priority-submit-2026-05-26.md` — operador submete; não há API pública.)

## Matriz baseline idioma × tipo × status
<!-- preenchida quando baseline-2026-05-26.json concluir -->
