# ── i18n: sync translations if pt-BR dictionaries changed ──
# Uses git diff --cached to detect changes; exits instantly if none found.
# Requires GEMINI_API_KEY in .env.local for actual translation.
node --env-file=.env.local scripts/i18n/sync-i18n.mjs 2>/dev/null || node scripts/i18n/sync-i18n.mjs

# ── SEO: validate structured data ──
npm run seo:validate

# ── SEO: validate Rich Results, DID & JSON-LD integrity ──
npm run seo:rich-results
